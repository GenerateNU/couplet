import React, {useState} from 'react';

export default function MatchHomeScreen({ navigation }) {

    // Code for figuring out if they have any matches 
    const [matches, setMatches] = useState([]);

    return (
        
    );
};
